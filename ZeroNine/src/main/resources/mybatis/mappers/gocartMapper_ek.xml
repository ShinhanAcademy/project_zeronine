<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.zeronine.gocart">
	<delete id="deleteCartItem" parameterType="int">
		delete from cart
		where "customerId" = cast(#{customerId} as UUID)
			AND "productId" = cast(#{productId} as UUID)
	</delete>
	<!-- <insert id="goProductCart" parameterType="CartVo"> 
		INSERT INTO public.cart( "customerId", "productId", "productCount")
			VALUES (CAST(#{custid} AS UUID), CAST(#{productId} AS UUID), 1);
	</insert>
	<select id="cartCheckPid" parameterType="String" resultType="CartVo">
		SELECT  "productId"
			FROM cart
		where "customerId" = CAST(#{custid} AS UUID);
	</select> -->
</mapper>